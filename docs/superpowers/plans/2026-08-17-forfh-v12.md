# ForFH V1.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** V1.2 dengan notif ringkasan "Besok" jam 20:00 WIB, varian widget tugas kedua (3 tugas terdekat + status sync), deep-link widget ke tab spesifik, dan guard sync saat logged out.

**Architecture:** Notif "Besok" mengikuti pola TASK_DEADLINE yang sudah terbukti: planner murni → row one-shot di Room (kind `DAY_PREVIEW`) → reconcile lewat `ReconcilePlanner` → handler guard di `AlarmFlowHandler` → notif biasa channel `reminder_tugas`. Widget kedua adalah kelas `GlanceAppWidget` terpisah yang baca Room langsung di `provideGlance` (pola `ForfhWidget`). Deep link memperluas mekanisme extra `open_tasks` yang sudah ada menjadi extra `open_tab` generik.

**Tech Stack:** Kotlin 2.4.0, Compose BOM 2026.08.00, Glance 1.1.1, Room 2.8.4, WorkManager 2.11.2, JUnit + kotlinx-coroutines-test.

**Spec:** Pembicaraan dengan user 2026-08-17 (scope di bawah). Tidak ada dokumen spec terpisah — plan ini adalah spec-nya.

## Global Constraints

- Branch `feat/v12` dari `main` (saat ini `fb39c67`, v1.1.0). Commit + push per task ke `feat/v12`. JANGAN merge/push `main` tanpa persetujuan user.
- Aturan alarm TIDAK berubah: offset per-hari, HANYA kuliah pertama per hari, snooze 3 mnt max 5x, mute per-hari hanya CLASS_ALARM.
- UI TANPA emoji; semua data dari Room/API, tidak ada hardcoded.
- Sesi murni logika wajib unit test (pola `TaskDeadlinePlannerTest`); VM test pakai fake DAO + `CompletableDeferred` gate (pola `TugasViewModelTest`).
- Unit test + `.\gradlew.bat :app:assembleRelease` hijau = gate per task. Verifikasi device TIDAK wajib (user handle sendiri; catat item device ke ledger).
- Rilis: versionCode 3, versionName "1.2.0".
- Glance 1.1.1: modifier dimensi WAJIB `.dp` (Int overload = resource ID → crash). Tidak ada `TextOverflow` di Glance.
- Jangan commit rahasia (keystore, token).
- Docs library via context7 MCP bila ragu API (Glance/Compose/Room).

## Scope final (kesepakatan user 2026-08-17)

1. Notif "Besok" 20:00 WIB — ringkasan jadwal besok + jumlah tugas deadline besok.
2. Widget deep link — tap widget jadwal buka tab Jadwal; tap widget tugas buka tab Tugas.
3. Varian widget kedua — 3 tugas terdekat + status sync terakhir.
4. Guard sync: periodic/one-shot sync tidak jalan (sukses senyap) saat logged out.
5. Rilis v1.2.0.

DI-CORET dari usulan awal (sudah ada di V1.1, diverifikasi eksplorasi 2026-08-17): halaman detail tugas + subtasks (`TugasDetailScreen` sudah render deskripsi & subtasks), 401 auto-logout + logout wipe (`SessionExpiryInterceptor` + `AppContainer.logout()`), background sync periodik (`SyncWorker.enqueuePeriodic` 6 jam dipanggil di `ForfhApp.onCreate`).

---

### Task 1: TomorrowPlanner murni (row DAY_PREVIEW 20:00 WIB)

**Files:**
- Create: `app/src/main/java/com/aryariap/forfh/sync/TomorrowPlanner.kt`
- Test: `app/src/test/java/com/aryariap/forfh/sync/TomorrowPlannerTest.kt`

**Interfaces:**
- Consumes: `ScheduleEntity` (field `dayOfWeek: Int`, `enabled: Boolean`), `ScheduledAlarmEntity`.
- Produces: `class TomorrowPlanner(zone: ZoneId)` dengan `fun computeDayPreview(schedules: List<ScheduleEntity>, now: ZonedDateTime): ScheduledAlarmEntity?` dan companion `fun dayPreviewIdentity(date: LocalDate): String = "tmrw|${date}"`. Kind barunya: `"DAY_PREVIEW"`. Trigger: `now.toLocalDate().atTime(20, 0).atZone(zone)` (hari INI jam 20:00 — notif malam ini tentang besok).

Aturan:
- Return row HANYA jika besok (`now.plusDays(1)`) punya ≥1 jadwal `enabled` dengan `dayOfWeek == besok.dayOfWeek.value`... PENTING: konvensi `ScheduleEntity.dayOfWeek` adalah `0=Minggu..6=Sabtu` yang SAMA dengan `java.time.DayOfWeek.getValue()` (Minggu=7 di java!). Konversi: `if (dow == 7) 0 else dow` — atau bandingkan `schedule.dayOfWeek == tomorrowDayIso` di mana `tomorrowDayIso = besok.dayOfWeek.getValue() % 7`.
- `triggerAtMillis > now` (R19: trigger masa lalu dilarang — lewat jam 20:00 hari ini → null).
- `skipDates`/mute TIDAK menyentuh DAY_PREVIEW (aturan yang sama dengan TASK_DEADLINE).

- [ ] **Step 1: Write failing tests** — contoh kasus: (a) besok Senin ada 2 jadwal Senin enabled → row id `"tmrw|<besok-yyyy-MM-dd>"`, kind DAY_PREVIEW, trigger = hari ini 20:00 WIB; (b) besok tidak ada jadwal → null; (c) sekarang 20:30 WIB → null (sudah lewat); (d) besok ada jadwal tapi `enabled=false` → null. Gunakan builder `wib(...)` pola `TaskDeadlinePlannerTest`:

```kotlin
class TomorrowPlannerTest {
    private val zone = ZoneId.of("Asia/Jakarta")
    private val planner = TomorrowPlanner(zone)

    private fun wib(s: String): ZonedDateTime =
        LocalDateTime.parse(s, DateTimeFormatter.ofPattern("yyyy-MM-dd'T'HH:mm")).atZone(zone)

    private fun schedule(dow: Int, enabled: Boolean = true) = ScheduleEntity(
        id = "s$dow", courseId = "c1", courseName = "MK", courseCode = "MK01",
        courseColor = "#3b82f6", lecturer = null, credits = 2, dayOfWeek = dow,
        startTime = "08:00", endTime = "09:40", room = null, onlineUrl = null, enabled = enabled,
    )

    @Test
    fun `besok ada kuliah - row DAY_PREVIEW hari ini jam 2000 WIB`() {
        // 2026-08-18 = Selasa (dayOfWeek java=2). now Senin 10:00.
        val now = wib("2026-08-17T10:00")
        val row = planner.computeDayPreview(listOf(schedule(2)), now)!!
        assertEquals("tmrw|2026-08-18", row.id)
        assertEquals("DAY_PREVIEW", row.kind)
        assertEquals(
            wib("2026-08-17T20:00").toInstant().toEpochMilli(), row.triggerAtMillis,
        )
    }
    // ... kasus b/c/d
}
```

- [ ] **Step 2: Run — expect FAIL (class not found)**
- [ ] **Step 3: Implementasi minimal**

```kotlin
package com.aryariap.forfh.sync

import com.aryariap.forfh.data.db.ScheduleEntity
import com.aryariap.forfh.data.db.ScheduledAlarmEntity
import java.time.LocalDate
import java.time.ZoneId
import java.time.ZonedDateTime
import java.time.format.DateTimeFormatter

/**
 * Murni: notifikasi ringkasan "Besok" (malam sebelum hari kuliah).
 * Row one-shot kind DAY_PREVIEW, trigger HARI INI jam 20:00 WIB — pola TaskDeadlinePlanner.
 * skipDates/mute TIDAK menyentuh row ini (aturan sama dengan TASK_DEADLINE).
 */
class TomorrowPlanner(private val zone: ZoneId = ZoneId.of("Asia/Jakarta")) {
    companion object {
        const val PREVIEW_HOUR = 20
        private const val PREFIX = "tmrw"
        private val DATE_FMT: DateTimeFormatter = DateTimeFormatter.ofPattern("yyyy-MM-dd")

        fun dayPreviewIdentity(date: LocalDate): String = "$PREFIX|${date.format(DATE_FMT)}"
    }

    fun computeDayPreview(schedules: List<ScheduleEntity>, now: ZonedDateTime): ScheduledAlarmEntity? {
        val tomorrow = now.toLocalDate().plusDays(1)
        // ScheduleEntity.dayOfWeek: 0=Minggu..6=Sabtu; java DayOfWeek: 1=Sen..7=Ming → %7
        val tomorrowDow = tomorrow.dayOfWeek.value % 7
        val hasClass = schedules.any { it.enabled && it.dayOfWeek == tomorrowDow }
        if (!hasClass) return null
        val trigger = now.toLocalDate().atTime(PREVIEW_HOUR, 0).atZone(zone)
        if (trigger.toInstant().toEpochMilli() <= now.toInstant().toEpochMilli()) return null // R19
        return ScheduledAlarmEntity(
            id = dayPreviewIdentity(tomorrow),
            kind = "DAY_PREVIEW",
            scheduleId = null,
            offsetMinutes = 0,
            occurrenceDate = tomorrow.format(DATE_FMT), // tanggal yang DIREVIEW (besok)
            triggerAtMillis = trigger.toInstant().toEpochMilli(),
            snoozeCount = 0,
        )
    }
}
```

Catatan: `occurrenceDate` menyimpan tanggal BESOK (yang direview), bukan hari ini — handler memakainya untuk query jadwal besok. Ini beda subtle dari TASK_DEADLINE (yang menyimpan deadlineDay); didokumentasikan di KDoc.

- [ ] **Step 4: Run — expect PASS**
- [ ] **Step 5: Commit** `feat: planner murni notif Besok (DAY_PREVIEW 20:00 WIB)`

---

### Task 2: Integrasi ReconcilePlanner (row DAY_PREVIEW di desired)

**Files:**
- Modify: `app/src/main/java/com/aryariap/forfh/sync/ReconcilePlanner.kt` (field `private val deadlinePlanner` + akhir `desiredRows`)
- Test: extend `app/src/test/java/com/aryariap/forfh/sync/ReconcilePlannerTest.kt` (jika tidak ada, buat baru mengikuti pola)

**Interfaces:**
- Consumes: `TomorrowPlanner.computeDayPreview(schedules, now)` dari Task 1.
- Produces: `desiredRows` kini juga memuat row `tmrw|*`; pass cancel fullRebuild otomatis membersihkan row basi (sudah ada — tidak perlu perubahan).

- [ ] **Step 1: Test gagal dulu** — computeOps dengan jadwal besok enabled → ada `AlarmOp.Schedule` dengan `row.kind == "DAY_PREVIEW"`; tanpa jadwal besok → tidak ada row DAY_PREVIEW; row DAY_PREVIEW trigger lama yang tersimpan di `current` dengan tanggal basi di-cancel saat fullRebuild.

- [ ] **Step 2: Run — expect FAIL**

- [ ] **Step 3: Implementasi** — di `ReconcilePlanner`:

```kotlin
private val tomorrowPlanner = TomorrowPlanner()
```

di akhir `desiredRows` (setelah blok TASK_DEADLINE, sebelum `return result`):

```kotlin
// Notifikasi "Besok" 20:00 WIB (TomorrowPlanner): skipDates/mute TIDAK menyentuh
// DAY_PREVIEW — pola yang sama dengan TASK_DEADLINE di atas.
tomorrowPlanner.computeDayPreview(schedules, now)?.let { result[it.id] = it }
```

(`computeDayPreview` sudah menjamin trigger future — cek R19 internal.)

- [ ] **Step 4: Run semua test ReconcilePlanner + TomorrowPlanner — PASS (287+ test tetap hijau)**
- [ ] **Step 5: Commit** `feat: integrate DAY_PREVIEW ke reconcile`

---

### Task 3: TomorrowSummaryText murni (isi notif)

**Files:**
- Create: `app/src/main/java/com/aryariap/forfh/sync/TomorrowSummaryText.kt`
- Test: `app/src/test/java/com/aryariap/forfh/sync/TomorrowSummaryTextTest.kt`

**Interfaces:**
- Consumes: `ScheduleEntity`, `TaskEntity`.
- Produces: `object TomorrowSummaryText { fun build(schedules: List<ScheduleEntity>, tasks: List<TaskEntity>, tomorrow: LocalDate, zone: ZoneId): String? }`. Null = tidak ada yang bisa dilaporkan.

Format yang diminta user (verbatim):
`Besok Senin: PIH 13:00 (A101), Ilmu Negara 15:00 · 1 tugas deadline`

Aturan:
- Judul nama hari Indonesia: Senin..Minggu (map lokal, bukan locale default OS — hasil harus deterministik di test).
- Kuliah: urut `startTime` asc; nama pakai `courseCode ?: courseName`; waktu `startTime` (format "HH:mm" sudah di entity); ruang `room` non-null → ` (${room})`; `onlineUrl` non-null → ` (Daring)`.
- Tugas: hitung task `status != "DONE"` dengan `dueAt` jatuh di besok ([besok 00:00, besok+lusa 00:00) WIB). >0 → akhiran ` · N tugas deadline` (N=1: "1 tugas deadline"; N>1: "N tugas deadline").
- Tidak ada kuliah besok TAPI ada tugas deadline besok → `"Besok Senin: tanpa kuliah · 1 tugas deadline"` (planner Task 1 tidak membuat row untuk kasus ini — sekaligus keputusan desain: notif "Besok" hanya untuk malam sebelum hari kuliah; TIDAK perlu ubah planner).

- [ ] **Step 1: Failing tests** — kasus: (a) 2 kuliah + 1 tugas → string verbatim di atas; (b) kuliah + 2 tugas → "· 2 tugas deadline"; (c) kuliah onlineUrl; (d) tanpa tugas → tanpa akhiran "·"; (e) urutan kuliah tidak sesuai input (acak) → terurut startTime.
- [ ] **Step 2: Run — FAIL**
- [ ] **Step 3: Implementasi minimal** (object murni, tanpa dependency Android).
- [ ] **Step 4: Run — PASS**
- [ ] **Step 5: Commit** `feat: teks ringkasan Besok`

---

### Task 4: Wiring alarm DAY_PREVIEW (action, handler, notif)

**Files:**
- Modify: `app/src/main/java/com/aryariap/forfh/alarm/AlarmReceiver.kt` (action baru + routing)
- Modify: `app/src/main/java/com/aryariap/forfh/alarm/AlarmScheduler.kt` (`actionFor` + `"DAY_PREVIEW" -> AlarmReceiver.ACTION_DAY_PREVIEW`)
- Modify: `app/src/main/java/com/aryariap/forfh/alarm/AlarmFlowHandler.kt` (`handleDayPreview`)
- Modify: `app/src/main/java/com/aryariap/forfh/alarm/ForfhNotifications.kt` (`showDayPreview`)
- Test: `app/src/test/java/com/aryariap/forfh/alarm/AlarmFlowHandlerDayPreviewTest.kt` (jika pattern test handler ada; jika handler lama tidak punya test unit, buat guard murni `DayPreviewDecision` mengikuti pola `DeadlineDecision` + test itu saja — handler tetap tipis)

**Interfaces:**
- Consumes: `TomorrowPlanner.dayPreviewIdentity`, `TomorrowSummaryText.build`, pola `handleTaskDeadline` (guard row/trigger/loggedIn → fire → cancel row).
- Produces: `AlarmReceiver.ACTION_DAY_PREVIEW = "com.aryariap.forfh.action.DAY_PREVIEW"`; `ForfhNotifications.showDayPreview(text: String, date: String)`.

Handler (mirror `handleTaskDeadline`, tanpa per-tugas):

```kotlin
suspend fun handleDayPreview(intent: Intent) {
    val date = intent.getStringExtra("occurrenceDate") // tanggal yang direview (besok)
    val trigger = intent.getStringExtra("triggerAtMillis")?.toLongOrNull()
    if (date == null || trigger == null) {
        AppLog.warn(TAG, "day preview extras tak valid date=$date trigger=$trigger")
        return
    }
    val identity = TomorrowPlanner.dayPreviewIdentity(LocalDate.parse(date))
    val row = alarmsDao.getByIdOnce(identity)
    if (row == null || row.triggerAtMillis != trigger) {
        AppLog.warn(TAG, "day preview ignore id=$identity row=${row != null}")
        return
    }
    if (!sessionManager.isLoggedIn()) {
        AppLog.warn(TAG, "day preview cancel-silent id=$identity (logout)")
        rescheduler.cancelAlarm(identity)
        return
    }
    val tomorrow = LocalDate.parse(date)
    val zone0 = zone
    val from = tomorrow.atStartOfDay(zone0).toInstant().toEpochMilli()
    val to = tomorrow.plusDays(1).atStartOfDay(zone0).toInstant().toEpochMilli()
    val text = TomorrowSummaryText.build(
        schedulesDao.getAllOnce(), tasksDao.getDueTasksOnce(from, to), tomorrow, zone0,
    )
    if (text != null && notifications.hasPermission()) {
        notifications.showDayPreview(text, date)
        AppLog.info(TAG, "day preview show date=$date")
    } else {
        AppLog.warn(TAG, "day preview skip-silent date=$date text=${text != null}")
    }
    // one-shot: fire/tidak → row dihapus; reconcile membangun ulang utk malam berikutnya
    rescheduler.cancelAlarm(identity)
}
```

`showDayPreview` (copy `showTaskDeadline` dengan perubahan: title "Besok", requestCode/key `StableHash.of("tmrw|$date")`, extra `open_tab = 0` (tab Jadwal) alih-alih `open_tasks`; channel `CHANNEL_TASK`, PRIORITY_DEFAULT, autoCancel).

Router `AlarmReceiver`:
```kotlin
ACTION_DAY_PREVIEW -> handler.handleDayPreview(intent)
const val ACTION_DAY_PREVIEW = "com.aryariap.forfh.action.DAY_PREVIEW"
```
`AlarmScheduler.actionFor`: tambah `"DAY_PREVIEW" -> AlarmReceiver.ACTION_DAY_PREVIEW`.

- [ ] **Step 1: Failing test** untuk guard murni (jika bikin `DayPreviewDecision`): row null → Ignore; trigger mismatch → Ignore; logout → CancelSilently; valid → Fire(text). Pola test `DeadlineDecisionTest`.
- [ ] **Step 2: FAIL → implement → PASS** (guard murni, lalu wiring Android tipis).
- [ ] **Step 3: Full unit test suite + `.\gradlew.bat :app:assembleRelease` hijau.**
- [ ] **Step 4: Commit** `feat: notif Besok 20:00 WIB (wiring penuh)`

---

### Task 5: Deep-link `open_tab` (widget → tab)

**Files:**
- Modify: `app/src/main/java/com/aryariap/forfh/MainActivity.kt` (baca extra Int)
- Modify: `app/src/main/java/com/aryariap/forfh/ui/ForfhAppRoot.kt` (param `startTab: Int?`)
- Modify: `app/src/main/java/com/aryariap/forfh/widget/ForfhWidget.kt` (clickable dengan parameters)
- Test: extend test VM/composable yang relevan jika ada; minimal unit test mapping extra (fungsi murni `StartTab.fromIntent(openTasks: Boolean, openTab: Int?): Int?`).

**Interfaces:**
- Produces: intent extra `"open_tab": Int` (0=Jadwal, 1=Tugas, 2=Info, 3=Atur). Backward-compat: extra `open_tasks=true` lama → tab 1. Glance: `actionStartActivity<MainActivity>(parameters = actionParametersOf("open_tab" to 0))` — parameter Glance masuk sebagai intent extras.

MainActivity:
```kotlin
val openTab = StartTab.fromIntent(
    openTasks = intent.getBooleanExtra("open_tasks", false),
    openTab = intent.getIntExtra("open_tab", -1),
)
intent.removeExtra("open_tasks"); intent.removeExtra("open_tab")
setContent { ForfhTheme { ForfhAppRoot(container = app.container, startTab = openTab) } }
```
`ForfhAppRoot`: ganti param `openTasks: Boolean` → `startTab: Int?`; `LaunchedEffect(startTab) { startTab?.let { tab = it } }` (tab index valid 0..3, di-clamp/diabaikan bila di luar). Gunakan fungsi murni `StartTab` agar logika bisa dites:

```kotlin
object StartTab {
    fun fromIntent(openTasks: Boolean, openTab: Int): Int? = when {
        openTab in 0..3 -> openTab
        openTasks -> 1
        else -> null
    }
}
```
Letakkan `StartTab` di `ui/StartTab.kt`, test unit biasa.

ForfhWidget kedua `clickable` (compact + standard):
```kotlin
.clickable(actionStartActivity<MainActivity>(parameters = actionParametersOf("open_tab" to 0)))
```
(Jadwal widget → tab 0. TasksWidget Task 6 pakai `to 1`. Notif `showTaskDeadline`/`showDayPreview` boleh sekalian dipindah ke `open_tab` — `open_tasks` tetap didukung backward-compat.)

- [ ] **Step 1: Failing test `StartTabTest`** (4 kasus: valid, invalid, openTasks legacy, none).
- [ ] **Step 2: FAIL → implement `StartTab` + MainActivity + ForfhAppRoot → PASS**
- [ ] **Step 3: Ganti clickable ForfhWidget; build release hijau; commit** `feat: deep-link open_tab dari widget`

---

### Task 6: TasksWidget — 3 tugas terdekat + status sync

**Files:**
- Create: `app/src/main/java/com/aryariap/forfh/widget/NextTasks.kt` (helper murni)
- Create: `app/src/main/java/com/aryariap/forfh/widget/TasksWidget.kt`
- Create: `app/src/main/java/com/aryariap/forfh/widget/TasksWidgetProvider.kt`
- Create: `app/src/main/res/xml/forfh_tasks_widget_info.xml`
- Modify: `app/src/main/AndroidManifest.xml` (receiver kedua)
- Modify: `app/src/main/java/com/aryariap/forfh/widget/WidgetUpdater.kt` (refresh kedua widget)
- Test: `app/src/test/java/com/aryariap/forfh/widget/NextTasksTest.kt`

**Interfaces:**
- Consumes: `tasksDao.getActiveByDeadline(): List<TaskEntity>` (sudah ada, terurut dueAt asc nulls last), `prefs.lastSyncAt()/lastSyncStatus()` via `SyncStateStore`, `UiFormat.deadline` (pola format deadline), pola `ForfhWidget` (provideGlance baca Room, background widgetBackground, padding `.dp`).
- Produces: `fun nextTasks(tasks: List<TaskEntity>, limit: Int = 3): List<TaskEntity>` (buang DONE — query sudah, tapi helper tahan input — ambil 3 pertama); `data class TasksWidgetData(tasks, lastSyncAt: Long, lastSyncStatus: String)`.

Helper murni:
```kotlin
fun nextTasks(tasks: List<TaskEntity>, limit: Int = 3): List<TaskEntity> =
    tasks.filter { it.status != "DONE" }.take(limit)
```
(juga fungsi murni `syncStatusLine(lastSyncAt: Long, lastSyncStatus: String, nowMs: Long): String` → `"Sinkron 14:02"` sukses / `"Gagal sinkron 14:02"` / `"Belum pernah sinkron"` — format jam `HH:mm` WIB dari epoch; deterministik via parameter, testable).

TasksWidget (mirror ForfhWidget):
- `sizeMode = SizeMode.Responsive(setOf(DpSize(150.dp, 110.dp), DpSize(260.dp, 110.dp)))`.
- Data di `provideGlance` via `withContext(Dispatchers.Default)`: `nextTasks(container.tasksDao.getActiveByDeadline())`, `container.syncState.lastSyncAt()/lastSyncStatus()`.
- Isi: judul kecil "Tugas terdekat"; maks 3 baris (title `maxLines=1`, warna dot `courseColor`, deadline format helper); baris status sync `syncStatusLine` tipis di bawah; kosong → "Tidak ada tugas aktif" (data nyata, bukan placeholder palsu).
- Background `.background(GlanceTheme.colors.widgetBackground)`, clickable FULL area sebelum padding:
  `actionStartActivity<MainActivity>(parameters = actionParametersOf("open_tab" to 1))`.

WidgetUpdater:
```kotlin
suspend fun refreshAll(context: Context) {
    try {
        ForfhWidget().updateAll(context)
        TasksWidget().updateAll(context)
    } catch (t: Throwable) {
        Log.w(TAG, "Refresh widget gagal (non-fatal)", t)
        AppLog.error(TAG, "refresh widget gagal (non-fatal): ${t.message}")
    }
}
```

Manifest (mirror receiver pertama, resource `@xml/forfh_tasks_widget_info`) + xml minWidth/minHeight sama seperti `forfh_widget_info.xml` (salin nilai existing), `updatePeriodMillis` 1800000, `initialLayout` sama pola.

- [ ] **Step 1: Failing tests** `NextTasksTest` (DONE dibuang, limit 3, urutan terjaga) + `syncStatusLine` (3 kasus).
- [ ] **Step 2: FAIL → implement helper murni → PASS**
- [ ] **Step 3: TasksWidget + provider + xml + manifest + WidgetUpdater; `assembleRelease` hijau.**
- [ ] **Step 4: Commit** `feat: widget varian tugas (3 tugas terdekat + status sync)`

---

### Task 7: Guard sync saat logged out

**Files:**
- Modify: `app/src/main/java/com/aryariap/forfh/sync/SyncWorker.kt` (doWork)
- Test: `app/src/test/java/com/aryariap/forfh/sync/SyncWorkerGuardTest.kt` — jika SyncWorker tidak punya seam test (butuh Context), taruh guard sebagai fungsi murni kecil `shouldSkipSync(isLoggedIn: Boolean, mode: String?): Boolean` di companion + test itu; wiring Android 3 baris.

**Interfaces:**
- Consumes: `app.container.sessionManager.isLoggedIn()` (sudah ada, suspend).
- Produces: sync (mode selain reconcile) saat logged out → `Result.success()` TANPA memanggil `syncRepository.sync()` (tidak ada percikan 401, tidak wipe data lokal oleh failure path).

```kotlin
else -> {
    if (!app.container.sessionManager.isLoggedIn()) {
        AppLog.info("SyncWorker", "sync skip: belum login (periodic/one-shot diam)")
        Result.success()
    } else {
        // ... body sync existing tidak berubah
    }
}
```
Mode reconcile TIDAK di-guard (reconcile dari Room lokal, tidak butuh sesi).

- [ ] **Step 1: Failing test fungsi guard murni** (logged out + mode sync → skip; logged out + reconcile → jalan; logged in → jalan).
- [ ] **Step 2: FAIL → implement → PASS; full suite hijau**
- [ ] **Step 3: Commit** `feat: guard sync saat logged out`

---

### Task 8: Rilis v1.2.0

**Files:**
- Modify: `app/build.gradle.kts` (versionCode 3, versionName "1.2.0")

- [ ] **Step 1: Full unit test suite + `.\gradlew.bat :app:assembleRelease` hijau.**
- [ ] **Step 2: Bump versionCode 3 / versionName "1.2.0"; build; salin APK `app\build\outputs\apk\release\app-release.apk` → `~/Downloads/ForFH-v1.2.0.apk`.**
- [ ] **Step 3: Commit** `release: v1.2.0`.
- [ ] **Step 4: Laporkan ke user; MERGE/PUSH MAIN HANYA SETELAH PERSETUJUAN USER. Device-check list (user handle sendiri): notif Besok 20:00 muncul malam hari kuliah, widget tugas muncul di picker & resize 2x2, tap kedua widget ke tab benar, periodic sync tidak 401 setelah logout.**

---

## Urutan & dependensi

Task 1 → 2 → 3 → 4 (rantai DAY_PREVIEW; 3 bisa paralel dengan 1-2). Task 5 → 6 (TasksWidget pakai `open_tab`). Task 7 independen. Task 8 terakhir.

## Self-review

- Coverage: 5 item scope → Task 1-4 (notif Besok), 5+6 (deep link & widget tugas), 7 (guard), 8 (rilis). Detail tugas/401/periodic sudah ada di V1.1 — dicoret, bukan gap.
- Tipe konsisten: kind `"DAY_PREVIEW"`, identity prefix `"tmrw|"`, extra `"open_tab"` dipakai seragam di Task 4/5/6.
- Tidak ada placeholder: semua kode langkah ditulis; implementer mengikuti file pola yang disebut.
