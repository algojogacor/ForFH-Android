<?xml version="1.0" encoding="utf-8"?>
<!--
  ForFH launcher icon — foreground
  Design: Abstract geometric scales
  Philosophy: Balance / law — abstracted to pure geometry.
  No gradients, no glow, no illustration. Flat white on transparent.
  Readable at 48dp and beautiful at 192dp.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- === BEAM (horizontal crossbar) === -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M22,49 H86 V55 H22 Z" />

    <!-- === CENTER PIVOT STEM (top knob) === -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M51,32 H57 V49 H51 Z" />

    <!-- Top pivot cap -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M47,28 H61 V34 H47 Z" />

    <!-- === LEFT HANG LINE === -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M22,55 H28 V66 H22 Z" />

    <!-- === RIGHT HANG LINE === -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M80,55 H86 V66 H80 Z" />

    <!-- === LEFT PAN (bottom bar) === -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M14,66 H38 V72 H14 Z" />

    <!-- === RIGHT PAN (bottom bar) === -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M70,66 H94 V72 H70 Z" />

    <!-- === BASE POLE === -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M51,55 H57 V78 H51 Z" />

    <!-- === BASE FOOT === -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M40,72 H68 V78 H40 Z" />

</vector>
